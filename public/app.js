let db=JSON.parse(localStorage.getItem("rvdb")||'{"insumos":[],"fichas":[]}'),tab="insumos";
const c=document.querySelector("#content"),money=n=>Number(n||0).toLocaleString("pt-BR",{style:"currency",currency:"BRL"});
function save(){localStorage.setItem("rvdb",JSON.stringify(db));render()}
function stats(){ni.textContent=db.insumos.length;nf.textContent=db.fichas.length;let a=db.fichas.filter(f=>f.preco>0).map(f=>f.custo/f.porcoes/f.preco*100);avg.textContent=(a.length?a.reduce((x,y)=>x+y,0)/a.length:0).toFixed(1)+"%"}
function render(){stats();document.querySelectorAll("nav button").forEach(b=>b.classList.toggle("active",b.dataset.tab===tab));
if(tab==="insumos")c.innerHTML='<div class="row"><h2>Banco de Insumos</h2><button class="primary" onclick="novoInsumo()">+ Insumo</button></div>'+db.insumos.map(i=>`<div class="card row"><div><b>${i.nome}</b><div class="muted">${i.unidade} · FC ${i.fc.toFixed(3)}</div></div><div class="price">${money(i.real)}</div></div>`).join("")+(db.insumos.length?"":'<div class="card muted">Cadastre o primeiro insumo.</div>');
else if(tab==="fichas")c.innerHTML='<div class="row"><h2>Fichas Técnicas</h2><button class="primary" onclick="novaFicha()">+ Ficha</button></div>'+db.fichas.map(f=>`<div class="card"><div class="row"><b>${f.nome}</b><b>${(f.preco?f.custo/f.porcoes/f.preco*100:0).toFixed(1)}% CMV</b></div><div class="muted">${f.porcoes} porções · custo ${money(f.custo)}</div></div>`).join("")+(db.fichas.length?"":'<div class="card muted">Crie a primeira ficha técnica.</div>');
else c.innerHTML='<h2>CMV</h2>'+db.fichas.map(f=>`<div class="card row"><div><b>${f.nome}</b><div class="muted">Custo/porção ${money(f.custo/f.porcoes)} · Venda ${money(f.preco)}</div></div><b>${(f.preco?f.custo/f.porcoes/f.preco*100:0).toFixed(1)}%</b></div>`).join("")+(db.fichas.length?"":'<div class="card muted">O CMV aparecerá após cadastrar fichas.</div>')}
window.novoInsumo=()=>{c.innerHTML='<h2>Novo insumo</h2><div class="card form"><input id="nome" placeholder="Ingrediente"><select id="un"><option>KG</option><option>L</option><option>G</option><option>ML</option><option>UN</option><option>PCT</option><option>CX</option></select><input id="pb" type="number" step=".001" placeholder="Peso bruto"><input id="pl" type="number" step=".001" placeholder="Peso líquido"><input id="pc" type="number" step=".01" placeholder="Preço de compra (R$)"><button class="primary wide" id="salvar">Salvar insumo</button></div>';salvar.onclick=()=>{let fc=Number(pb.value)/Number(pl.value)||1;db.insumos.push({nome:nome.value,unidade:un.value,fc,real:Number(pc.value)*fc});save()}};
window.novaFicha=()=>{c.innerHTML='<h2>Nova ficha</h2><div class="card form"><input id="fn" placeholder="Nome do prato"><select id="cat"><option>Entrada</option><option>Prato Principal</option><option>Acompanhamento</option><option>Molho</option><option>Sobremesa</option><option>Bebida</option><option>Outros</option></select><input id="por" type="number" placeholder="Porções"><input id="cus" type="number" step=".01" placeholder="Custo total (R$)"><input id="pv" type="number" step=".01" placeholder="Preço de venda (R$)"><textarea id="prep" placeholder="Modo de preparo"></textarea><button class="primary wide" id="sf">Salvar ficha</button></div>';sf.onclick=()=>{db.fichas.push({nome:fn.value,categoria:cat.value,porcoes:Number(por.value)||1,custo:Number(cus.value),preco:Number(pv.value),preparo:prep.value});save()}};
document.querySelectorAll("nav button").forEach(b=>b.onclick=()=>{tab=b.dataset.tab;render()});render();